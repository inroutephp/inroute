before_commands:
    - composer install

filter:
    excluded_paths: [vendor/*, bin/*, example/*]

tools:
    php_cpd:
        enabled: true
        excluded_dirs: [vendor, bin, example]
    php_pdepend:
        enabled: true
        excluded_dirs: [vendor, bin, example]
    php_loc:
        enabled: true
        excluded_dirs: [vendor, bin, example]
    php_code_coverage: true
    php_mess_detector: true
    php_code_sniffer:
        enabled: true
        command: phpcs
        config:
            standard: PSR2
        filter:
            excluded_paths:
                - 'vendor/*'
                - 'bin/*'
                - 'example/*'
    php_analyzer: true
